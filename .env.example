TMDB_API_KEY=
AUTH_ENCRYPTION_KEY=
# At least 24 characters — the app refuses to boot on a shorter one.
# Generate with: openssl rand -hex 16
ADMIN_SETUP_TOKEN=
APP_ORIGIN=http://localhost:3000
TRUSTED_PROXY_HOPS=0
PORT=3000
DATA_DIR=./data
# PUID=999
# PGID=999
